import { ChartHostDirective } from './chart-host.directive';

describe('ChartHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartHostDirective();
    expect(directive).toBeTruthy();
  });
});
