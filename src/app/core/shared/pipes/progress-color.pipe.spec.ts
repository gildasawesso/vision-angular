import { ProgressColorPipe } from './progress-color.pipe';

describe('ProgressColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgressColorPipe();
    expect(pipe).toBeTruthy();
  });
});
